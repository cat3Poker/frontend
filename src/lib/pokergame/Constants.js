import { KeyboardButtonModel } from "./models/KeyboardButtonModel.js";

// Enumeration for gameplay mode type IDs
export const GameplayModeTypeIDs = {
  SIT_AND_GO: 0,
  TOURNAMENT: 1
};

// Enumeration for avatar availability IDs
export const AvatarAvalibilityIDs = {
  REMOVED: 0,
  INACTIVE: 1,
  ACTIVE: 2,
  FOLDED: 3,
  ALL_IN: 4
};

